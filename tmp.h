

/*
#define defconststring(name)

#define _textMake(name, litstr) \
	static const char name ## Literal[] = litstr; \
	const ecctextstring_t io_libecc_text_##name = { .bytes = name ## Literal, .length = sizeof(name ## Literal) - 1 }

*/

